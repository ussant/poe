# POE

### How to start
1. run `yarn` or `npm`
2. ios - `cd ios && pod install && cd ..` and then `yarn ios` or `npm run ios`
3. android - `yarn android` or `npm run android`

### Used libraries
1. React Native Reanimated
